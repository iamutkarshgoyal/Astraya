# Product Asset Staging

Use this folder for incoming original product photography. Production catalog
files must be exported to `images/products/<product-slug>/` so the existing API
and jsDelivr URLs continue to work without a database migration.
